#include <iostream>
#include <fstream>
using namespace std;


struct edificio
{
    float enero;
    float febrero;
    float marzo;
    float abril;
    float mayo;
    float junio;
};

void Cargar (edificio Ed[], int n)
{
    for (int i = 0 ; i < n ; i++)
    {
        cout << "\n---------- INGRESE LAS GANACIAS POR MES - EDIFICIO " << i + 1 << " -------------\n";
        cout << "Enero: ";
        cin >> Ed[i].enero;
        cout << "Febrero: ";
        cin >> Ed[i].febrero;
        cout << "Marzo: ";
        cin >> Ed[i].marzo;
        cout << "Abril: ";
        cin >> Ed[i].abril;
        cout << "Mayo: ";
        cin >> Ed[i].mayo;
        cout << "Junio: ";
        cin >> Ed[i].junio;
    }
}
/*
void Mostrar(edificio Ed[], int n)
{
    for (int i = 0 ; i < n ; i++)
    {
        cout << "\n---------- INGRESE LAS GANACIAS POR MES  " << i + 1 << " -------------\n";
        cout << "Enero: "<<Ed[i].enero<<endl;
        cout << "Febrero: "<<Ed[i].febrero<<endl;
        cout << "Marzo: "<<Ed[i].marzo<<endl;
        cout << "Abril: "<<Ed[i].abril<<endl;
        cout << "Mayo: "<<Ed[i].mayo<<endl;
        cout << "Junio: "<<Ed[i].junio<<endl;
    }
}


*/

void guardarTexto(edificio Ed[], int n)
{
    ofstream archTexto;
    string nuevoarchivo;
    cout<<"Ingrese el nombre del archivo que desea crear y la extension"<<endl;
    getline(cin,nuevoarchivo);
    archTexto.open(nuevoarchivo.c_str(),ios::out);

    archTexto << "====== REGISTRO DE GANANCIAS ======\n";
    for (int i = 0; i < n; i++)
    {

        archTexto <<Ed[i].enero<< endl;
        archTexto <<Ed[i].febrero<< endl;
        archTexto <<Ed[i].marzo<< endl;
        archTexto <<Ed[i].abril<< endl;
        archTexto <<Ed[i].mayo<< endl;
        archTexto <<Ed[i].junio<< endl;

        archTexto << "-----------------------------------\n";
    }

    archTexto.close();
    cout << "\nGanancias guardadas correctamente.\n";

}


void leer (edificio Ed [], int n)
{
    string cadenapantalla, nombre_archivo;
    ifstream archivo;

    cout <<"\nIngresa el nombre del archivo que deseas leer: "<<endl;
    getline(cin, nombre_archivo);
    archivo.open(nombre_archivo.c_str(), ios::in);

        if (archivo.fail())
    {
        cout << "No existe el archivo" << endl;
        return;
    }

    while (!archivo.eof())
    {
        for (int i = 0 ; i < n ; i ++)
        {
        float total = 0 ;
        getline(archivo,cadenapantalla);
        total = total + cadenapantalla;

        }


    }

    cout << "\n----- CONTENIDO DEL ARCHIVO -----\n";
    while (getline(archivo, cadenapantalla))
    {
        cout <<"TOTAL : "<< total << endl;
    }

    archivo.close();
}
}




int main()
{
    edificio Ed[3];
    Cargar(Ed, 3);
    Mostrar(Ed, 3);

    guardarTexto(Ed, 3);
    leer(Ed, 3);

    cout << "\nProceso finalizado correctamente \n";
    return 0;
}
